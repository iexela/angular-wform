name: Make Candidate 
on:
  push:
    branches: [test/master]
    paths:
      - 'projects/core/**'
jobs:
  validate:
    uses: ./.github/workflows/testing.yml
    with:
      ref: ${{github.ref}}
  stable:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - run: echo make candidate