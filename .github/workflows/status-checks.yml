name: Status Checks
on: [workflow_call]
jobs:
  validate-pr:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
  # testing:
  #   uses: ./.github/workflows/testing.yml
  #   with:
  #     ref: ${{github.ref}}
