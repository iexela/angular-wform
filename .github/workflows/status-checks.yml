name: Status Checks
on: [pull_request]
jobs:
  validate-pr:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
  # testing:
  #   uses: ./.github/workflows/testing.yml
  #   with:
  #     ref: ${{github.ref}}
