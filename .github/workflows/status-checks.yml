name: Status Checks
on: [pull_request]
jobs:
  validate-pr:
    uses: iexela/angular-wform/.github/workflows/testing.yml@test/master
    with:
      ref: ${{github.ref}}
      base_ref: origin/${{github.base_ref}}
